export type ProjectData = {
  id: string;
  createdAt: string;
  updatedAt: string;
  title: string;
  description: string;
export type CreateProjectType = {
  title: string;
  description: string;
};
